# Routes
# This file defines all application routes (Higher priority routes first)
# https://www.playframework.com/documentation/latest/ScalaRouting
# ~~~~

# An example controller showing a sample home page
GET         /graphiql                        controllers.AppController.graphiql

POST        /appointments                    controllers.GameController.createGameAppointment
#PUT         /appointments/:id/players        controllers.GameController.p
GET         /appointments/:id                controllers.GameController.findAppointmentById(id: String)
